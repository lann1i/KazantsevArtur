<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout android:orientation="vertical" android:layout_width="match_parent" android:layout_height="wrap_content">

        <!-- Ввод -->
        <EditText android:id="@+id/et_title" android:hint="Название книги" android:layout_width="match_parent" android:layout_height="wrap_content"/>
        <EditText android:id="@+id/et_author" android:hint="Автор" android:layout_width="match_parent" android:layout_height="wrap_content"/>
        <Button android:id="@+id/btn_add" android:text="Добавить книгу" android:layout_width="match_parent" android:layout_height="wrap_content"/>

        <!-- Чтение -->
        <Button android:id="@+id/btn_read" android:text="Читать из провайдера" android:layout_width="match_parent" android:layout_height="wrap_content"/>

        <!-- Вывод -->
        <TextView android:id="@+id/tv_output" android:layout_width="match_parent" android:layout_height="wrap_content"
            android:layout_marginTop="16dp" android:textSize="16sp"/>

    </LinearLayout>
</ScrollView>